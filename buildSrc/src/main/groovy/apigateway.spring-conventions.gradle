plugins {
    id 'org.springframework.boot'
    id 'apigateway.spring-dependency-conventions'
}

dependencies {
    implementation "org.springdoc:springdoc-openapi-ui:${openApiUiVersion}"
}

tasks.named('test') {
    useJUnitPlatform()
}
